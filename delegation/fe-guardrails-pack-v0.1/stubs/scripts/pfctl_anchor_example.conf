# /etc/pf.anchors/fragments_engine.conf (example)
# Block all outbound by default for user 'fe-agent', then allow specific hosts.
# Load with: echo "anchor \"fragments_engine\"" | sudo pfctl -f -
#           echo "load anchor \"fragments_engine\" from /etc/pf.anchors/fragments_engine.conf" | sudo pfctl -f -

# table <fe_allow> persist { api.github.com }
# block out quick user fe-agent
# pass out quick proto { tcp } to <fe_allow> port { 80 443 } user fe-agent keep state
