name: Recall
slug: recall
version: 1.0.0
requires: { secrets: [], capabilities: ["search.query"] }
steps:
  - id: q
    type: transform
    template: "{{ ctx.body }}"
  - id: search
    type: search.query
    with: { query: "{{ steps.q.output }}", limit: 10 }
