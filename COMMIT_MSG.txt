feat(cleanup): Remove 11 unused models and fix Prism streaming telemetry

Model Cleanup (66 → 55 models, -16.7%):
- Remove 9 models with 0 references (AgentVector, ArticleFragment, CalendarEvent, 
  FileText, FragmentTag, ObjectType, PromptEntry, Thumbnail, WorkItemEvent)
- Remove SeerLog (unnecessary Fragment wrapper, just use Fragment with type='obs')
- Remove Article (test-only, no production usage)
- Remove ArticleStatus enum and SeerLogController

Critical Bug Fix:
- Fix PrismProviderAdapter::streamChat() to return final response with usage data
- Previously returned nothing, breaking token/cost telemetry for all Prism streaming
- Now returns model, provider, finish_reason, and usage metrics (prompt_tokens, 
  completion_tokens, total_tokens, cached_tokens)
- Enables proper cost tracking and telemetry for OpenAI, Anthropic, Ollama, OpenRouter
  when using Prism (AI_USE_PRISM=true)

Code Updates:
- Update FragmentTest to use Fragment instead of SeerLog
- Remove articleFragments() relationship from Fragment model
- Fix CompactProjectPicker import in ChatToolbar.tsx
- Remove unused React imports from 5 modal components

Documentation Created (delegation/tasks/cleanup-2025-10-14/):
- Complete systems inventory (24 systems documented)
- Detailed migration plans for Sprint→OrchestrationSprint and WorkItem→OrchestrationTask
- Cleanup opportunities analysis (18 categories)
- OrchestrationBug implementation confirmation
- Comprehensive action items and quick reference guides

All removed code backed up to backup/models/ and backup/
TypeScript build passes cleanly
Application verified functional

Fixes streaming telemetry regression introduced in PR #82
See delegation/tasks/cleanup-2025-10-14/FINAL_SUMMARY.md for complete details
